set( PACKAGE_VERSION "@MUJINVISIONCONTROLLERCLIENT_VERSION@" )
if( "${PACKAGE_FIND_VERSION}" VERSION_EQUAL "@MUJINVISIONCONTROLLERCLIENT_VERSION@")
  set(PACKAGE_VERSION_EXACT 1)
  set(PACKAGE_VERSION_COMPATIBLE 1)
endif()

if( "${PACKAGE_FIND_VERSION}" VERSION_LESS "@MUJINVISIONCONTROLLERCLIENT_VERSION@")
  set(PACKAGE_VERSION_COMPATIBLE 1)
endif()
